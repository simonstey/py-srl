# Rule Format Example 4: RULE/WHERE with Multiple Patterns
# Demonstrates Rule1 with complex body pattern

PREFIX ex: <http://example.org/>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

# Infer grandparent relationship
RULE {
    ?x ex:grandparent ?z .
} WHERE {
    ?x ex:parent ?y .
    ?y ex:parent ?z .
}
